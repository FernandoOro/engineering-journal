# 2026-08-28 (Friday)

**Context:** a company-wide security training session in the morning, followed by a network outage at
home that meant finishing the morning's work from a different location — the day's first commit landed
around midday. From there, work continued without interruption into the afternoon: brought the calendar
feature's data layer to real data on mobile (a direct continuation of the day before), fixed a real bug
in an overview screen, and added cross-cutting tests for the calendar wiring. Two pull requests opened
and merged the same day, both with tests green.

## What I did

### 1. Calendar feature: real data on mobile
Wired the mobile data layer's content-listing methods to real data behind the existing demo-mode seam.

### 2. Overview screen fix
Fixed a demo-mode flag and an upcoming-items count that had drifted out of sync with each other.

### 3. Cross-cutting tests
Added tests covering the calendar wiring across both changes above.

## Ops
Two pull requests merged, two issues closed. One issue from the day before stayed open into the
following week.

## Learnings / technical decisions
The overview-screen bug surfaced only once the calendar feature switched from mock to real data —
another case of a mock hiding a real inconsistency until it was replaced with the genuine data source.

## What I could do better
About 20 minutes of real work this morning wasn't logged as its own entry in the moment — a direct
consequence of the morning's disruption. Even so, the mobile side of the calendar feature closed out
complete, with tests green, by the end of the week.
