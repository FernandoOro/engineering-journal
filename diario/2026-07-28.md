# 2026-07-28 (Tuesday)

**Context:** rebuilt the entire local dev environment from scratch — a permanent OS switch to fix
yesterday's blocker, plus quick housekeeping that didn't depend on the broken setup.

After yesterday's total blocker, I decided to install Linux permanently on the machine instead of
continuing to fight Windows + WSL2's virtualization layers. Before starting the reinstall, I used the
early morning to close out housekeeping that didn't require the broken environment (merges done via
the GitHub web UI). The rest of the day went into backing up personal files, the OS install, and
reconfiguring three separate project environments from scratch.

## What I did

### 1. Quick housekeeping — 3 merges via GitHub (before touching the environment)
- Merged three already-approved branches from last week (an active-project store, new client-SDK
  methods, and real per-conversation IDs replacing a single-thread workaround). None of these required
  the local environment, so I closed them before starting the reinstall.

### 2. Backed up personal files
- Backed up my personal documents and files before proceeding with the install, to avoid losing
  anything in the process.

### 3. Installed Linux as the permanent OS
- Switched to Linux as the machine's permanent operating system instead of continuing to depend on
  WSL2 on top of Windows — the underlying goal was eliminating the nested virtualization layer that
  fully blocked yesterday.

### 4. Reconfigured three project environments from scratch
- **Mobile app** (pnpm/Turbo monorepo): Node, pnpm, and native Android tooling — SDK, platform/build
  tools, and an emulator running natively without an intermediate hypervisor, plus the mobile
  framework's CLI.
- **Backend**: Python, a virtual environment, and the project's pinned dependencies.
- **Docker**: installed and configured for both repos (local service containers already used by the
  team in dev).
- This step was only partially verified by end of day — confirming the Android emulator and mobile
  tooling work end-to-end without yesterday's RAM problems was left for tomorrow.

### 5. Operational documentation (evening, once the basic environment responded)
- Aligned the AI-model roster and fallback chain across both repos' collaboration/agent docs, added a
  project-level model-routing config to both, and logged per-developer work audits to one repo's
  changelog.

## Ops
- 3 merges (housekeeping, no new code of my own).
- 6 commits of operational documentation (no applicable tests — these are config/docs files, not
  product code).
- 0 new product features. The dev environment was installed but not verified end-to-end.

## Learnings / technical decisions
- Installing Linux permanently addresses the root cause of yesterday's blocker (nested
  virtualization), not a symptom — the Android SDK/emulator and mobile tooling run natively without
  competing for the same hypervisor.
- Migrating OS/machine means reconfiguring several distinct project environments (JS/Node, Python,
  Docker, Android SDK) — not a single step, and each one carries its own risk of being left
  misconfigured without explicit verification.

## What I could do better
- The day went almost entirely into personal infrastructure, with 0 lines of product code and 0 tests
  run against any of the three projects. Combined with yesterday, that's two consecutive days without
  feature progress — I need to confirm first thing tomorrow that the environment genuinely works
  end-to-end before assuming the problem is solved.
